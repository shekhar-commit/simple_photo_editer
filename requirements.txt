
streamlit==1.40.1  # Latest stable (supports NumPy 2.0+)
opencv-python==4.10.0.84  # Latest stable for better compatibility
numpy==2.0.0  # Keep for Python 3.13 support
Pillow==11.0.0  # Latest stable (fixed the build issue)
setuptools==75.6.0  # Latest stable
